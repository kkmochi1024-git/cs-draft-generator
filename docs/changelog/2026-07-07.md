# 変更ログ: 2026-07-07

## 変更概要

- **変更日時**: 2026-07-07
- **変更対象**:
  - `.git/`（新規作成: Gitリポジトリ初期化）
  - `.gitignore`（更新）
  - `settings/INDEX.md`（更新）
- **変更種別**: 新規作成 / 更新
- **変更カテゴリ**: 設定変更 / ドキュメント更新
- **変更理由**: プロジェクトがバージョン管理下になくロールバック不可能な状態だったため、Gitリポジトリを初期化。また、D-002（.emlリーダー）は2026-06-24に実装完了・全タスク完了済みだったが、設計書インデックスのステータスが「作成中」のまま更新漏れになっていたため修正

## 変更前

- `.git/` が存在せず、バージョン管理されていなかった
- `.gitignore` に `.coverage`・`.DS_Store` が含まれておらず、追跡対象に混入する状態だった
- `settings/INDEX.md` の D-002 のステータスが「作成中」（最終更新 2026-06-17）のままだった

## 変更後

- `git init -b main` で Git リポジトリを初期化（デフォルトブランチ: main）
- `.gitignore` に `.coverage` と `.DS_Store` を追加。`.env`・`data/`・`.steering/` は従来どおり除外され、PII・機密情報が追跡対象外であることを確認済み
- `settings/INDEX.md` の D-002 のステータスを「実装済み」、最終更新を 2026-07-07 に更新

## 影響範囲

- プロジェクト全体がバージョン管理下に入り、変更のロールバックが可能になった
- テストフィクスチャ（`tests/fixtures/*.eml`）は架空データ（山田太郎・ダミー電話番号）のみでありコミット可能と確認済み
- ソースコードの動作に影響なし

---

## 変更概要

- **変更日時**: 2026-07-07
- **変更対象**:
  - `CLAUDE.md`（更新）
  - `AGENTS.md`（更新）
  - `.claude/skills/log-app-change/SKILL.md`（更新）
  - `.agents/skills/log-app-change/SKILL.md`（更新）
- **変更種別**: 更新
- **変更カテゴリ**: 設定変更 / ドキュメント更新
- **変更理由**: Claude Code カスタム設定の全体レビューで発見した問題の修正。①CLAUDE.md と AGENTS.md（および `.claude/skills/` と `.agents/skills/`）が複製関係にあるのに同期ルールが明文化されておらず、片方だけ更新されると乖離するリスクがあった。②log-app-change スキルが旧スキル名 `/log-change` を参照していた（グローバル側は `log-global-change` に改名済みのため参照切れ）

## 変更前

- CLAUDE.md / AGENTS.md に複製関係・同期義務の記載なし
- `.claude/skills/log-app-change/SKILL.md` が `/log-change` を3箇所で参照（description 2箇所・本文見出し1箇所）
- `.agents/skills/log-app-change/SKILL.md` も同様に `/log-change` を参照し、さらに `~/.Codex/` という誤った大文字表記が2箇所あった

## 変更後

- CLAUDE.md / AGENTS.md の冒頭に「メンテナンス注意」を追記し、両ファイルおよび `.claude/skills/`・`.agents/skills/` の対を同期して更新するルールを明文化
- `/log-change` 参照をすべて `/log-global-change` に修正（両コピー）
- `.agents/` 側の `~/.Codex/` を `~/.codex/` に修正

## 影響範囲

- log-app-change スキルの SKIP 案内が実在するスキル名を指すようになり、カスタム資材変更時の記録先誘導が正しく機能する
- CLAUDE.md 変更時に AGENTS.md への反映が促され、Claude Code と Codex 等の間で指示内容の乖離を防げる
- アプリ本体（src/・tests/）への影響なし

---

## 変更概要

- **変更日時**: 2026-07-07
- **変更対象**: `.claude/settings.json`（更新）
- **変更種別**: 更新
- **変更カテゴリ**: 設定変更
- **変更理由**: カスタム設定レビューで発見した残存権限の掃除（ユーザー承認済み）。別プロジェクト（claude-code-book-chapter8）へのRead許可と、`~/.claude/skills/` 配下へのEdit許可3件は本プロジェクトでは不要で、後者はグローバルの settings.local.json の `Edit(~/.claude/*)` と機能的に重複していた

## 変更前

- `permissions.allow` に他プロジェクト参照1件＋グローバルスキルEdit許可3件が残存

## 変更後

- `permissions.allow` を空配列に整理

## 影響範囲

- 本プロジェクトでの権限プロンプト挙動に実質的変化なし（該当パスへのアクセスは本プロジェクトの作業で発生しないため）
