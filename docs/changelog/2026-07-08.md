# 変更ログ: 2026-07-08

## 変更概要

- **変更日時**: 2026-07-08
- **変更対象**:
  - `src/etl/pdf_reader.py`（新規）
  - `src/rag/index_service.py`（更新）
  - `src/rag/query_service.py`（更新）
  - `src/main.py`（更新）
  - `src/config.py`（既存の COLLECTION_EMAILS / COLLECTION_MANUALS を活用。今回変更なし）
  - `pyproject.toml`（更新: pymupdf 追加）
  - `tests/test_pdf_reader.py`（新規）
  - `tests/test_rag.py`（更新）
  - `settings/INDEX.md`（更新）
  - `settings/03-update-pdf-manual.md`（更新: ステータス実装済み）
  - `.steering/20260708-pdf-manual/`（新規: requirements/design/tasklist）
- **変更種別**: 新規作成 + 更新
- **変更カテゴリ**: 機能追加
- **変更理由**: 設計書 D-003（第2弾アップデート）に基づき、PDF形式の製品マニュアルをRAGに取り込み、マニュアル記載事項にも回答できるようにするため。

## 変更前

- RAGの参照ソースはメール（.eml / テキスト）由来の対応履歴のみ。
- `IndexService` / `QueryService` は `support_emails` 単一コレクションにハードコード。
- マニュアル（PDF）の内容は参照できず、仕様・操作手順の質問に正確に回答できなかった。

## 変更後

- **PDFリーダー追加**（`src/etl/pdf_reader.py`）: PyMuPDF(fitz) でページ単位テキスト抽出。空白ページスキップ、ページ番号のみの行（ヘッダー/フッター）の簡易除去、単一ファイル/ディレクトリ一括対応。既存 `eml_reader.py` と同じ構造（dataclass + Reader + 例外時の安全な返り値）。
- **IndexService の複数コレクション対応**: コンストラクタに `collection_name` 引数を追加（未指定時は既存互換で `COLLECTION_NAME`=support_emails にフォールバック）。`register_pages(pages, file_name)` を新設し、全ページ結合→マスキング1回→チャンク分割し、各チャンクの由来ページを `page="12"` または範囲 `"12-13"` でメタデータ付与。マスキングによる文字数変化はオフセット補正（`_orig_to_masked_pos`）で吸収。
- **QueryService の横断検索対応**: `support_emails` と `product_manuals` の両コレクションを `similarity_search_with_score` で検索し、距離昇順にマージ・内容で重複排除して上位 `SEARCH_TOP_K` 件を採用。
- **CLI拡張**（`src/main.py`）: `register --pdf <path>` / `register --pdf-dir <dir>` を追加。PDFはマニュアル専用コレクション（product_manuals）に登録。参照元表示を `_format_source` で整形（📄 file.pdf (p.23) / 📧 メール）。
- **依存追加**: `pymupdf>=1.24`（実インストールは 1.28.0）。
- **テスト**: `tests/test_pdf_reader.py` 新規（PyMuPDFでフィクスチャPDFを動的生成。ページ抽出/空白スキップ/不正・存在しないファイル/ディレクトリ一括/ページ範囲算出ロジック）。`tests/test_rag.py` の QueryService モックを `similarity_search_with_score` に更新。
- **品質**: pytest 53件全パス、ruff クリーン。register_pages はモックで実動作確認済み（ページ番号割当・PIIマスキング・オフセット補正）。

## 影響範囲

- **回答生成フロー**: 全質問がメール履歴に加えマニュアルも検索対象になる（挙動変更）。両コレクションが空でも従来どおり登録促しメッセージを返す。
- **既存の登録経路（テキスト/.eml）**: `IndexService` のデフォルトコレクションは従来どおり support_emails のため後方互換。
- **検索インターフェース**: `QueryService` 内部が `similarity_search` → `similarity_search_with_score` に変更。外部公開APIの `ask()` シグネチャは不変。
- **未検証事項**: Ollama 接続を要する `register --pdf` → `ask` の実データE2Eは未実施（要 Ollama 環境）。日本語PDFのフィクスチャは helv フォント制約のため未使用。

---

# 変更ログ: 2026-07-08（2件目）

## 変更概要

- **変更日時**: 2026-07-08
- **変更対象**:
  - `settings/04-update-web-ui.md`（更新）
  - `settings/06-update-llm-switch.md`（新規）
  - `settings/INDEX.md`（更新）
- **変更種別**: 更新 + 新規作成
- **変更カテゴリ**: 設計変更 / ドキュメント更新
- **変更理由**: D-004 レビューでユーザーから2点の指摘（①フロントエンドをReact/TSにすべきか ②PDFアップロード機能の欠落）。検討の結果、①はStreamlit維持とし移行判断基準を記録、②はD-004が6/17作成でD-003（6/19設計・7/8実装）を反映していなかった欠陥と判断し改訂。あわせて精査で発見した不整合（モジュール名乖離・未実装のクラウドLLM切替UI・非ストリーミングAPI）も修正。

## 変更前

- D-004: サイドバーに .eml/テキスト登録のみ（PDFなし）。LLMバックエンド切替UIがスコープ内だがバックエンド未実装。参照元表示が `metadata['subject']` 決め打ち（PDFメタデータ非対応）。アーキテクチャ図のモジュール名が実コードと乖離（masking/pipeline・rag/chain・rag/indexer）。ストリーミングは `chain.stream()` 前提で実APIと不一致。
- D-006: 存在せず。
- INDEX.md: D-006 の行・命名規則なし。

## 変更後

- **D-004 改訂**:
  - スコープにPDFマニュアルアップロード追加。参照元表示をメール/PDF両対応（📧/📄・ページ番号）に変更
  - §2 に「React + TypeScript への移行判断基準」を記録（Streamlit維持の理由3点＋移行検討条件5点＋二段階移行方針）
  - LLMバックエンド切替をスコープ外とし D-006 へ分離
  - アーキテクチャ図を実モジュール名（masking/service・rag/query_service・rag/index_service・etl/pdf_reader）に修正
  - ストリーミングは `QueryService.ask_stream()` 追加実装が必要と明記（unmaskはチャンク境界分断を考慮し最終確定文のみ）
  - `_format_source` の共通モジュール化（rag/formatting.py 案）、PII含む一時ファイルの登録後削除を注意点に追加
  - サイドバーの件数表示をコレクション別（メール/マニュアル）に変更
- **D-006 新設**: LLMバックエンド切替（ローカルOllama/クラウドClaude）。LLMファクトリ案・PII保護の前提（マスキング済みプロンプトのみ外部送信・mapping非送信のテスト保証）・Embeddingはローカル維持（ChromaDB互換性のため）・APIキー未設定時のUI無効化。ステータス: 作成中
- **INDEX.md**: D-006 行追加、D-004 最終更新を 2026-07-08 に、命名規則に `06` 追加

## 影響範囲

- 設計書のみの変更でありコードへの影響なし
- D-004 実装時のタスク量が変化: PDFアップロード・ask_stream・formatting共通化が追加、LLM切替UIが除外
- D-004 実装の前提が明確化（QueryService へのストリーミングメソッド追加が必要）
- 将来のReact移行判断が属人化せず設計書で追跡可能に

---

# 変更ログ: 2026-07-08（3件目）

## 変更概要

- **変更日時**: 2026-07-08
- **変更対象**:
  - `CLAUDE.md` / `AGENTS.md`（更新: 複製注記に「ツール名読み替えは意図的差分」を補足）
  - `.claude/skills/log-app-change/SKILL.md`（更新: argument-hint の YAML バグ修正）
  - `.agents/skills/log-app-change/SKILL.md`（更新: 同上＋Codex 側に存在しないスキルへの参照を除去）
  - `.claude/skills/save-chat/SKILL.md` / `.agents/skills/save-chat/SKILL.md`（更新: トリガー語句整合＋argument-hint 引用符化）
- **変更種別**: 更新
- **変更カテゴリ**: 設定変更 / バグ修正
- **変更理由**: カスタム設定の第三者レビュー（独立サブエージェント・10観点）で検出された指摘の修正（ユーザー承認済み）

## 変更前

- `log-app-change` の `argument-hint: [変更ファイルパス] [変更種別] [変更理由]` が YAML 構文エラーとなり **frontmatter 全体が無効化**。スキル一覧の表示が本文見出しの約100字切り詰めにフォールバックし、TRIGGER/SKIP の後半が自動発火の判断に届いていなかった
- `.agents`（Codex 用）の log-app-change が、Codex 環境には存在しない `/log-global-change` を SKIP 案内で参照していた
- save-chat の description と本文見出しでトリガー語句が不一致（「〜記録して下さい」「このやり取りを残して」が片方ずつ）
- CLAUDE.md/AGENTS.md の複製注記で、意図的なツール名差分の扱いが未定義（同期チェック時の誤検出リスク）

## 変更後

- argument-hint を引用符付き（`"[...]"`）に修正。修正直後にスキル一覧が frontmatter description 表示に復帰することを実測確認
- `.agents` 側の SKIP 案内を実在参照に依存しない表現（「資材側の変更ログはツール側の運用に従う」）に変更
- save-chat のトリガー語句を description・本文見出しの両方で統一（両コピー）
- 複製注記に「ツール名・ツール固有パスの読み替え（Claude→Codex 等）は意図的差分」と補足

## 影響範囲

- log-app-change・save-chat の自動発火が本来の設計どおり機能する（log-app-change はプロジェクト絶対ルールの根幹）
- アプリ本体（src/・tests/）への影響なし
- グローバル資材側の対応修正は `~/.claude/steering/第三者レビュー指摘の全件修正-2026-07-08.md` に記録（コミット 81e66a4）

---

# 変更ログ: 2026-07-08（4件目）

## 変更概要

- **変更日時**: 2026-07-08
- **変更対象**:
  - `src/app/__init__.py` / `src/app/main.py`（新規: Streamlit エントリポイント）
  - `src/app/components/__init__.py` / `chat.py` / `sidebar.py` / `masking_preview.py`（新規: UI部品）
  - `src/rag/formatting.py`（新規: 参照元表示整形の共通モジュール）
  - `src/rag/query_service.py`（更新: ask_stream 追加・共通処理抽出）
  - `src/rag/models.py`（更新: StreamingAnswer 追加）
  - `src/main.py`（更新: _format_source を rag/formatting.py へ移設）
  - `pyproject.toml`（更新: streamlit 追加）
  - `tests/test_formatting.py`（新規）/ `tests/test_rag.py`（更新: ストリーミングテスト追加）
  - `settings/INDEX.md` / `settings/04-update-web-ui.md`（更新: ステータス実装済み）
  - `.steering/20260708-web-ui/`（新規: requirements/design/tasklist）
- **変更種別**: 新規作成 + 更新
- **変更カテゴリ**: 機能追加
- **変更理由**: 設計書 D-004（第3弾アップデート・2026-07-08改訂版）に基づき、CUIベースの回答生成を Streamlit のチャットUIに置き換え、サポート担当者が直感的に使えるインターフェースを提供するため。

## 変更前

- 回答生成はCLI（`python -m src.main ask/chat`）のみで、非エンジニアには敷居が高かった。
- `QueryService` は非ストリーミング（`invoke` 一括取得）のみ。
- 参照元表示の整形ロジック（`_format_source`）が `src/main.py` 内のプライベート関数で、GUIから再利用できなかった。

## 変更後

- **Streamlit UI 新設**（`src/app/`）: `streamlit run src/app/main.py` で起動。チャットUI（`st.chat_message`/`st.chat_input`）、会話履歴（session_state）、ストリーミング逐次表示、参照元 expander（📧 メール / 📄 PDF+ページ）、サイドバーで .eml/PDF/テキスト登録とコレクション別チャンク数表示、テキスト登録前のマスキング確認ダイアログ（`@st.dialog`）。
- **ストリーミング対応**（`query_service.py` + `models.py`）: `ask_stream()` が `StreamingAnswer`（source_documents/mapping を先に確定、tokens はマスク済みチャンクを遅延yield）を返す。逐次表示はマスク済みテキスト、生成完了後に一括 unmask（マスクトークンのチャンク境界分断対策）。`ask()` との共通処理は `_build_prompt`/`_wrap_connection_error` に抽出し、既存 `ask()` の挙動は不変。
- **整形共通化**: `_format_source` を `src/rag/formatting.py::format_source` へ移設。CUI は cmd_ask/cmd_chat 内の遅延import（`mask` コマンドの軽量起動を維持するため）。
- **PII保護**: アップロードファイルの一時保存は `finally` で必ず削除。登録・回答生成とも既存マスキング経路を再利用。
- **重い初期化のキャッシュ**: GiNZA・LLMクライアントは `st.cache_resource` で再ロード回避。
- **依存追加**: `streamlit>=1.38`（実インストール 1.59.0）。
- **テスト**: format_source 7件 + ask_stream 3件を追加し **63件全パス**。ruff クリーン。AppTest によるアプリ実行スモーク（例外なし・各要素レンダリング確認）と実サーバー起動（HTTP 200）も確認。

## 影響範囲

- **CUI**: `ask`/`chat` の参照元表示は formatting.py 経由に変わったが出力は同一（回帰テストでパス）。`mask` コマンドの遅延ロード設計も維持。
- **QueryService**: `ask()` の外部挙動は不変。`ask_stream()` は新規追加のため既存呼び出しに影響なし。
- **未検証事項**: Ollama 接続環境での E2E（ストリーミング回答・アップロード登録の実データ確認）は未実施。D-003 の未検証事項と合わせて実施推奨。sidebar の登録純粋関数のユニットテストは次回追加を検討。

---

# 変更ログ: 2026-07-08（5件目）

## 変更概要

- **変更日時**: 2026-07-08
- **変更対象**:
  - `Dockerfile`（更新: デフォルトCMDをWebUIに変更・EXPOSE 8501）
  - `compose.yaml`（更新: ポート公開・ボリューム拡大）
  - `CLAUDE.md` / `AGENTS.md`（更新: コマンド欄にWebUI起動・PDF登録例を追記、両ファイル同期）
  - `settings/04-update-web-ui.md`（更新: §8 にDocker起動方法と構成変更を追記）
- **変更種別**: 更新
- **変更カテゴリ**: 設定変更
- **変更理由**: ユーザーからの「D-004までの機能をDockerで実行できるか」の確認で、Docker構成がD-004（WebUI）に未対応であることが判明したため（ポート未公開・CMDがCUI固定・入力ファイル受け渡し経路なし）。

## 変更前

- Dockerfile の CMD が `python -m src.main chat`（CUI固定）で、WebUIを起動する手段がなかった
- compose.yaml に `ports` がなく、コンテナ内の Streamlit にブラウザから到達できなかった
- ボリュームが `./data/chroma_db` のみで、CLI登録（`register --pdf` 等）の入力ファイルをコンテナへ渡す経路がなかった
- イメージが D-002 時点のビルドで pymupdf / streamlit 未インストール

## 変更後

- **Dockerfile**: デフォルトCMDを `streamlit run src/app/main.py --server.address=0.0.0.0 --server.port=8501 --server.headless=true` に変更。`docker compose up -d` だけでWebUIが起動する。CUIは従来通り `docker compose exec` で利用可能
- **compose.yaml**: `ports: "127.0.0.1:8501:8501"`（**PIIを扱うためlocalhost限定バインド**。LAN上の他マシンからのアクセスを遮断）。ボリュームを `./data:/app/data` に拡大（chroma_db 永続化＋CLI登録用入力ファイルの受け渡し）
- **ドキュメント**: CLAUDE.md/AGENTS.md のコマンド欄と D-004 §8 にDocker起動方法を追記
- **検証済み**: イメージ再ビルド成功（pymupdf/streamlit は pyproject 経由で導入）。コンテナ内で ①WebUI HTTP 200 + ヘルスチェックOK ②GiNZAマスキング動作（Person/EMAIL検出） ③`register --pdf/--pdf-dir` CLI存在 ④app モジュール import 正常、を確認

## 影響範囲

- **運用変更**: `docker compose up -d` の挙動が「CUI chat 待機」から「WebUI起動」に変わる。CUIは `docker compose exec -it app python -m src.main chat` で従来通り
- **セキュリティ**: 8501 は localhost からのみアクセス可能。LAN公開が必要になった場合は compose.yaml のバインドを変更する（その際は認証の要件化＝React移行判断基準の再評価と合わせて検討）
- **データ**: chroma_db のマウントパスは実質不変（`./data` 配下として引き続き永続化）。既存DBはそのまま利用可能
- **未検証事項**: コンテナ内からの Ollama 接続（`host.rancher-desktop.internal:11434`）を要する回答生成E2Eは未実施（ホストで Ollama 起動後に確認）

---

# 変更ログ: 2026-07-08（6件目）

## 変更概要

- **変更日時**: 2026-07-08
- **変更対象**:
  - `settings/00-architecture.md`（削除）
  - `settings/product-requirements.md`（更新: PIIマスキング検出率のKPIに誤検出率・継続改善の運用を追記）
  - `settings/architecture.md`（更新: コンテナビルド方針を追記）
  - `settings/development-guidelines.md`（更新: クロスプラットフォーム実装規約を追記）
  - `settings/01-prototype.md` 〜 `settings/06-update-llm-switch.md`（移動: `settings/phases/` へ）
  - `settings/INDEX.md`（更新: phases/ へのリンク変更、D-000行削除、命名規則更新）
  - `settings/repository-structure.md`（更新: ディレクトリツリー・命名規則表をphases/構成に更新）
  - `.steering/20260708-settings-reorg/`（新規: requirements/design/tasklist）
- **変更種別**: 削除 + 更新 + 移動
- **変更カテゴリ**: 設計変更 / ドキュメント更新
- **変更理由**: settings/ 直下に「フェーズ別設計書（連番）」と「マスター仕様書（/init-docs生成）」の性質が異なる14ファイルが混在し見通しが悪かった。加えて `00-architecture.md`（2026-06-17作成の一枚岩マスター設計書）が、その後 /init-docs で生成された4文書（PRD・機能設計・アーキテクチャ・リポジトリ構造）に内容が分解・引き継ぎ済みで陳腐化していた（Outlook連携がスコープ外化後も残存、ディレクトリ構成が実コードと不一致、技術スタック表に PyMuPDF/httpx/beautifulsoup4 が欠落）。ユーザー依頼により2段階評価（Opus→Fable による指摘の相互検証）を経て、固有内容を移植した上での廃止と、フェーズ設計書のサブフォルダ化を承認。

## 変更前

- `settings/` 直下に14ファイルがフラットに存在し、`00-architecture.md`（陳腐化した旧マスター設計書）と `architecture.md`（現行）という同名文書が2つ存在していた
- `01-prototype.md` が前提として `00-architecture.md` を参照
- INDEX.md・repository-structure.md が旧構成（00あり・phases/なし）を前提に記述

## 変更後

- **00-architecture.md 固有内容の移植**（削除前に実施）:
  - PRDのセカンダリーKPIに「誤検出率も算出」「検出漏れパターンを分析しルールを継続改善」の2点を追記
  - development-guidelines.md に「クロスプラットフォーム実装規約」（pathlib一貫使用・改行コード吸収）を追記
  - architecture.md に「コンテナビルド方針」（GiNZAモデルのビルド時取り込み理由）を追記
  - 正規表現パターン表・.emlエンコーディング検出は既存コード/02の記述で担保済みのため移植対象外と判断
- **00-architecture.md 廃止**: `git rm` で削除。01-prototype.md の前提リンクを `../architecture.md`・`../product-requirements.md` に差し替え
- **フェーズ設計書のサブフォルダ化**: `settings/phases/` を新設し、01〜06の6ファイルを移動（01,03,04,05は既存追跡ファイルのため `git mv` でrename、02は`git mv`でrename、06は元々未追跡ファイルだったため通常`mv`+`git add`で移動しA(新規追加)扱いとなった）
- **INDEX.md**: 冒頭にphases/とマスター仕様書の配置ルールを追記、設計書一覧のリンクを`phases/...`形式に更新、D-000行を削除、命名規則から`00`行を削除し末尾に廃止経緯を追記
- **repository-structure.md**: ディレクトリツリーのsettings/ブロックをphases/構成に更新（従来欠落していた06・glossaryも反映）、ファイル命名規則表のフェーズ別設計書行に配置先`settings/phases/`を明示

## 影響範囲

- コードへの影響なし（設計書のみの変更）
- `settings/`直下はINDEX.md＋マスター仕様書6ファイル＋`phases/`のみとなり、`/init-docs`系スキルが前提とするマスター仕様書のパス（`settings/architecture.md`等）は変更していないため既存スキルへの影響なし
- 過去の記録（docs/changelog/の他日付・docs/chat-logs/・.steering/の他フォルダ）は時点記録として意図的に未変更（旧パス`00-architecture.md`等への言及が残るのは正しい状態）
- CLAUDE.md/AGENTS.mdはフェーズ設計書への直接参照がないことを確認済みのため変更不要と判断
- 検証: `00-architecture`参照が生きた文書からゼロ件であること、phases/内の相互リンクが全て解決すること、`git status`でrename/delete/addの内訳が想定通りであることを確認済み
