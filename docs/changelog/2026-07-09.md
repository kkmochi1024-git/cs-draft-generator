# 変更ログ: 2026-07-09

## 変更概要

- **変更日時**: 2026-07-09
- **変更対象**:
  - `.claude/git-sync-policy.md`（新規）
  - `.steering/20260709-git-sync-policy/`（新規: requirements/design/tasklist）
- **変更種別**: 新規作成
- **変更カテゴリ**: 設定変更
- **変更理由**: グローバルスキル /git-sync を「エンジン（共通手順）＋リポジトリ別ポリシー」構成に改修したことに伴い、本プロジェクト用のポリシーファイルを新設した。改修の発端は、旧 git-sync をこのフォルダから呼ぶと実行場所を検証せずに本リポジトリへコミットしうる構造的欠陥が判明したこと（2026-07-09 調査）。エンジン側の改修内容は `~/.claude/steering/git-syncのエンジンポリシー分離とモデル分担ポリシー導入-2026-07-09.md` に記録。

## 変更前

新規作成のため該当なし（本リポジトリには git-sync 用の安全弁定義が存在せず、グローバル /git-sync の誤動作リスクがあった）

## 変更後

- **`.claude/git-sync-policy.md`**: /git-sync エンジンが本リポジトリで動くためのポリシー定義
  - `expected-remote`: 想定リモートURLを定義（エンジンが照合し、不一致なら停止＝ポリシー取り違え・誤リポジトリ実行の防止）
  - mode: blacklist。追加禁止パターン: `data/`（PII生データ・ChromaDB）・`.steering/`・`.eml`・`.pdf`
  - 既知の無害ヒット: `tests/fixtures/` のダミー .eml、`src/masking/`・`tests/test_masking.py` のPII検出パターン、`docs/` のエラーハンドリング説明語
  - 備考: PII生データのコミット禁止（CLAUDE.md 開発ルール）、.env.example の更新忘れ注意、コミット前の /log-app-change 記録漏れ確認
- **`.steering/20260709-git-sync-policy/`**: 本作業の計画・実施記録（計画=Fable、実施=Sonnet worker、検証=Fable の分担で実施）

## 影響範囲

- アプリ本体（src/・tests/）への影響なし
- 本リポジトリで /git-sync が利用可能になった（従来は安全に使えなかった）。push は従来どおりユーザー承認必須
- `data/`・`.eml`・`.pdf` がステージされた場合は /git-sync が検知して停止する（tests/fixtures/ のダミーデータは無害確認の上で続行可）
- `.claude/git-sync-policy.md` は Git 追跡対象として本リポジトリにコミットされる想定（次回コミットに含める）

---

# 変更ログ: 2026-07-09（2件目）

## 変更概要

- **変更日時**: 2026-07-09
- **変更対象**:
  - `README.md`（新規）
  - `.gitignore`（更新: `.steering/` 行の復元）
- **変更種別**: 新規作成 + 更新
- **変更カテゴリ**: ドキュメント更新 / 設定変更
- **変更理由**: README はリポジトリ構造定義書で「後日作成」とされていたものをユーザー依頼で作成。GitHub への初 push が完了しリポジトリの入口となる説明が必要になった。.gitignore は、コミット作業中に `.steering/` 行が過去の作業で削除されていたことを発見（CLAUDE.md 開発ルール「`.steering/` は Git 管理外」・開発ガイドライン・git-sync ポリシーと矛盾し、changelog に削除の記録もないため意図しない変更と判断）し復元した

## 変更前

- README.md: 存在しなかった
- .gitignore: `.steering/` の行が欠落（`?? .steering/` として未追跡表示される状態）

## 変更後

- **README.md**: プロジェクト概要（PII マスキング→RAG→LLM の価値提案）、機能一覧（実装済み✅／設計中🚧 D-005・D-006）、アーキテクチャ図（コンテナ＋ホスト Ollama）、セットアップ手順（Ollama モデル取得・.env 準備・Rancher Desktop / Docker Desktop 別の OLLAMA_BASE_URL 注記・compose 起動）、WebUI と CLI の使い方（実装済みサブコマンド mask/register/ask/chat のみ記載）、テスト、ドキュメントへの導線、データ取り扱い（data/ Git 管理外・マスキング済みのみ保存・マッピング非永続）
- **.gitignore**: `.env`・`data/` の次に `.steering/` を復元。HEAD と同一内容に戻ったため差分なし

## 影響範囲

- コードへの影響なし
- README 記載のコマンド・環境変数は `src/main.py` の argparse 定義・`.env.example`・`compose.yaml` と照合済み（未実装の sync-zendesk・クラウド切替は記載せず「設計中」と明示）
- .gitignore 復元により `.steering/` が Git 管理外に戻り、git-sync ポリシーの禁止パターンとの整合が回復

---

# 変更ログ: 2026-07-09（3件目）

## 変更概要

- **変更日時**: 2026-07-09
- **変更対象**:
  - `.gitignore`（更新: `.steering/` → `.steering/ignore/`）
  - `CLAUDE.md` / `AGENTS.md`（更新: 開発ルールのGit管理外記述）
  - `.claude/git-sync-policy.md`（更新: 追加禁止パターンと説明）
  - `settings/development-guidelines.md`（更新: Git管理外ファイル一覧）
  - `settings/repository-structure.md`（更新: ディレクトリツリー注記）
- **変更種別**: 更新
- **変更カテゴリ**: 設定変更 / ドキュメント更新
- **変更理由**: ステアリングファイルの管理方針をユーザー指示で変更。機能開発に関するステアリングファイル（作業計画）は履歴として Git で追跡し、システムの機能と無関係な作業（ドキュメント整理・Git運用設定等）のステアリングのみ `.steering/ignore/` に置いて管理外とするため。`.steering/` 全体が管理外である前提が複数ドキュメントに明記されていたため、整合を保つよう一括更新した

## 変更前

- .gitignore: `.steering/` 全体を除外
- CLAUDE.md / AGENTS.md 開発ルール: 「`data/`、`.steering/` はGit管理外」
- git-sync-policy.md 追加禁止パターン: `(^|/)\.steering/`（`.steering/` 全体を遮断）
- development-guidelines.md / repository-structure.md: `.steering/` をGit管理外と記載

## 変更後

- .gitignore: 除外対象を `.steering/ignore/` のみに限定
- CLAUDE.md / AGENTS.md: 機能開発のステアリングは `.steering/` 直下（Git管理対象）、機能と無関係な作業のみ `ignore/` に置くルールを明記
- git-sync-policy.md: 追加禁止パターンを `(^|/)\.steering/ignore/` に変更し、説明も同趣旨に更新。既知の無害ヒットに `.steering/` 配下の技術用語 `token` 等を追記（追跡対象化に伴う初回 git-sync で無害ヒットを確認したため）
- development-guidelines.md / repository-structure.md: 同方針に記述を統一

## 影響範囲

- コード（src/・tests/）への影響なし
- 既存の機能系ステアリング4フォルダ（`20260621-プロトタイプ`・`20260624-eml-reader`・`20260708-pdf-manual`・`20260708-web-ui`、計12ファイル）が新たに未追跡として現れ、次回コミットの対象になる。`ignore/` 配下（`20260708-settings-reorg`・`20260709-git-sync-policy`）は引き続き管理外であることを `git check-ignore` で確認済み
- 追跡対象となる4フォルダに実PII（メールアドレス・電話番号の実値）が含まれないことを grep で確認済み（ヒットはPIIマスキング機能の仕様記述のみ）
- /git-sync 実行時、ステアリングファイルは `ignore/` 配下のみ遮断される動作に変わる
