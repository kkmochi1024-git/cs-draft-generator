# 変更ログ: 2026-06-23

## 変更概要

- **変更日時**: 2026-06-23
- **変更対象**:
  - `tests/test_ner_masker.py`（新規作成）
  - `src/masking/ner_masker.py`（更新）
- **変更種別**: 新規作成 / 更新
- **変更カテゴリ**: バグ修正 / テスト追加
- **変更理由**: NerMaskerのユニットテスト追加（プロトタイプ振り返りで指摘されたテスト不足の解消）。テスト作成の過程で、TARGET_LABELSに `Location` を指定していたがGiNZAは地名を `Province`/`City`/`Country` で返すため地名が一切検出されないバグを発見・修正

## 変更前

- NerMaskerのユニットテストが存在しなかった
- `TARGET_LABELS = {"Person", "Location"}` — GiNZAは `Location` ラベルを使用しないため、地名（東京都、大阪市等）が検出対象外になっていた

## 変更後

- **tests/test_ner_masker.py**: 9テストを追加
  - 人名検出（単数・複数）、地名検出、Organization除外、PII未検出テキスト、空文字列、位置情報の正確性、トークン連番、GiNZA未ロード時のフォールバック
  - GiNZAがロードできない環境では `@pytest.mark.skipif` で自動スキップ
- **src/masking/ner_masker.py**: `TARGET_LABELS` を `{"Person", "Province", "City", "Country"}` に修正。GiNZAが実際に返すラベルに合わせた

## 影響範囲

- 地名（都道府県名・市区町村名・国名）がマスキング対象として正しく検出されるようになった
- 既存テスト14件 + 新規9件 = 全23件パス

---

## 変更概要

- **変更日時**: 2026-06-23
- **変更対象**:
  - `tests/test_rag.py`（新規作成）
  - `src/rag/index_service.py`（更新）
- **変更種別**: 新規作成 / 更新
- **変更カテゴリ**: テスト追加 / バグ修正
- **変更理由**: IndexService/QueryServiceの統合テスト追加（プロトタイプ振り返りで指摘されたテスト不足の解消）。テスト作成の過程で空テキスト登録時のChromaDBエラーを発見・修正

## 変更前

- IndexService/QueryServiceのテストが存在しなかった（Ollama依存のためモックが必要）
- `IndexService.register("")` で空チャンクをChromaDBに渡そうとしてValueErrorが発生するバグ

## 変更後

- **tests/test_rag.py**: 9テストを追加
  - IndexService: 登録結果の型確認、PIIマスキング確認、カスタムメタデータ、統計情報取得、空テキスト登録（5件）
  - QueryService: 回答生成、ChromaDB空時のガイダンスメッセージ、PIIアンマスク確認、検索結果がLLMプロンプトに含まれることの確認（4件）
  - OllamaEmbeddings/ChatOllama/Chromaをモックし、外部接続なしで実行可能
- **src/rag/index_service.py**: チャンク分割結果が空の場合は早期リターンするガードを追加

## 影響範囲

- 全32テストパス（RegexMasker 7 + MaskingService 7 + NerMasker 9 + IndexService 5 + QueryService 4）
- 空テキスト登録が安全に処理されるようになった

---

## 変更概要

- **変更日時**: 2026-06-24
- **変更対象**:
  - `src/etl/__init__.py`（新規作成）
  - `src/etl/eml_reader.py`（新規作成）
  - `src/config.py`（更新: SUPPORT_DOMAIN追加）
  - `src/main.py`（更新: --eml / --eml-dir / --support-domain追加）
  - `pyproject.toml`（更新: beautifulsoup4追加）
  - `tests/test_eml_reader.py`（新規作成）
  - `tests/fixtures/inquiry.eml, response.eml, standalone.eml`（新規作成）
- **変更種別**: 新規作成 / 更新
- **変更カテゴリ**: 機能追加
- **変更理由**: 第1弾アップデート（D-002）の実装。.emlファイルからメール本文を自動抽出しRAGに登録する機能

## 変更前

- テキスト手動入力のみでRAG登録が可能
- .emlファイルの読み取り機能なし

## 変更後

- **src/etl/eml_reader.py**: ParsedEmail/EmailThread/EmlReaderクラスを実装。.emlパース（ヘッダー+本文抽出）、マルチパート対応（text/plain優先→text/html BeautifulSoupテキスト化）、エンコーディングフォールバック（UTF-8→ISO-2022-JP→Shift_JIS→CP932）、In-Reply-To/Referencesによるスレッド構築、サポートドメインによる問い合わせ/回答ペア判定、循環参照ガード付き
- **src/main.py**: registerコマンドに `--eml`（単一ファイル）、`--eml-dir`（ディレクトリ一括）、`--support-domain`（ペア判定用ドメイン）を追加。ペア登録時はsubject/date/thread_id/roleのメタデータを付与
- **src/config.py**: SUPPORT_DOMAIN設定を追加（環境変数で指定可能）
- **テスト**: eml_reader 9テスト（パース、ディレクトリ、スレッド検出、ペア判定）+ 既存32テスト = 全41テストパス

## 影響範囲

- .emlファイルからの一括データ登録が可能になった
- ChromaDBのメタデータにsource="eml"、subject、date、thread_id、roleが付与される
- 既存のテキスト手動入力による登録機能に変更なし
